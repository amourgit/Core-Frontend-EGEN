export {
  resolveRealm, getCurrentRealm, getRealm,
  getAdminBase, getOidcBase, extractSubdomain,
  clearRealmCache, resolveRealmFromRequest,
} from '@egen/esm-auth';
export type { ResolvedRealm } from '@egen/esm-auth';

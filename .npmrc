# Force le hoisting strict de react/react-dom pour éviter les instances dupliquées
# dans les workspaces npm — cause de l'erreur "usingClientEntryPoint undefined"
# lors de l'utilisation de Module Federation avec createRoot()
prefer-dedupe=true
legacy-peer-deps=false